# Optional. When set, RECAST uses OpenAI for analysis and generation.
# When unset, a local analysis engine still produces real opportunities and drafts from the source.
OPENAI_API_KEY=
OPENAI_MODEL=gpt-4o-mini
